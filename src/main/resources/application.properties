spring.application.name=TAbanking

# Server configuration
server.port=8080
server.servlet.context-path=/api/v1/

# Datasource configuration
spring.datasource.url=${RDB_URL:jdbc:postgresql://localhost:5432/banking_db}
spring.datasource.username=${RDB_USERNAME:postgres}
spring.datasource.password=${RDB_PASSWORD:757575}
spring.datasource.driver-class-name= org.postgresql.Driver

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#Security Configration
jwtSecret-${jwtSecret:e04b07479504128767a2507494c083aa16c43ed9938182b37bd2364b840d9b74
}
